"""callscope web UI — local server for driving callscope against a project.

Optional feature: install with `uv tool install 'callscope[ui]'` (or run with
`uv run --extra ui callscope ui`). Everything operates on paths local to the
machine the server runs on; bind to localhost unless you know what you want.
"""
