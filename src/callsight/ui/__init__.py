"""callsight web UI — local server for driving callsight against a project.

Optional feature: install with `uv tool install 'callsight[ui]'` (or run with
`uv run --extra ui callsight ui`). Everything operates on paths local to the
machine the server runs on; bind to localhost unless you know what you want.
"""
