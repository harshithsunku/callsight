"""tracekit web UI — local server for driving tracekit against a project.

Optional feature: install with `uv tool install 'tracekit[ui]'` (or run with
`uv run --extra ui tracekit ui`). Everything operates on paths local to the
machine the server runs on; bind to localhost unless you know what you want.
"""
